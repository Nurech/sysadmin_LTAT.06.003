apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-lab11
  namespace: lab11
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  defaultBackend:
    service:
      name: service-lab11
      port:
        number: 80
